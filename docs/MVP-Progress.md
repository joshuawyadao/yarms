# MVP progress

| Milestone | State | Branch | PR | Gates / next step |
| --- | --- | --- | --- | --- |
| 1. Foundation, share, player validation | Merged into `main` at `57e84c8` | `feat/foundation-share-player` | [#1](https://github.com/joshuawyadao/yarms/pull/1) | Codex review complete with no final findings; CI Verify and Repository Verify passed |
| 2. Saving and library | Merged into `main` at `ea01404` | `feat/saving-library-oembed` | [#2](https://github.com/joshuawyadao/yarms/pull/2) | Final Codex review complete with no findings; CI Verify and Repository Verify passed |
| 3. Player and notes | Merged into `main` at `b47b658` | `feat/workout-player-notes` | [#3](https://github.com/joshuawyadao/yarms/pull/3) | Final Codex review complete; CI Verify and Repository Verify passed |
| 4. Backup and polish | Merged into `main` at `265e3c8` | `feat/backup-polish` | [#4](https://github.com/joshuawyadao/yarms/pull/4) | Codex review, Brooks review, and required checks cleared before merge; final `main` build and 53 tests passed |
| 5. Free Personal Team sharing | Merged into `main` at `a7c83a4` | `codex/free-shortcut-sharing` | [#5](https://github.com/joshuawyadao/yarms/pull/5) | Direct TikTok sharing and inline playback worked on iPhone 18 Pro Max; required review and checks passed before merge. |
| 6. Compact player controls and UI automation | PR review in progress | `codex/compact-player-ui-tests` | [#6](https://github.com/joshuawyadao/yarms/pull/6) | Full-width video, 44-point controls, and a bottom-anchored TikTok fallback pass on a clean iPhone 18 Pro Max simulator. The isolated UI-test revision passes 59 unit tests and 3 UI tests (2 opt-in skips), repository verification, and a signed generic iPhone build. Fresh Codex review and CI Verify are pending. |

The four original MVP milestones and free Personal Team sharing follow-up are merged. The current follow-up addresses physical-iPhone layout feedback and repeatable UI automation. The review ledger below records historical App Group validation from the original implementation; [Shortcut-Sharing.md](Shortcut-Sharing.md) describes the current direct Share Sheet flow.

## Review ledger

- PR #1 Brooks review: a shared text payload could contain another URL before the TikTok video URL. The parser now scans all URL candidates; the regression test and five existing tests pass.
- PR #1 Codex review: the shared Xcode scheme lacked a Run/Profile executable. The generator and checked-in scheme now select the Yarms app, and repository verification checks this invariant. CI Verify passed on `20bfcef`; the addressed comment received a thumbs-up reaction.
- PR #1 Codex review: the iPhone web view did not explicitly allow inline media playback. The player now enables it, six local tests pass, and the device checklist calls for live inline-playback validation. CI Verify passed on `4300018`; the addressed comment received a thumbs-up reaction.
- PR #1 Codex review: the extension plist used fixed version strings. Both versions now inherit build settings; the built app and extension match, and repository verification checks the source plist. The final `f3f5a12` review found no further issues, both checks passed, and the PR was squash-merged.
- Milestone 2 local validation: 16 simulator tests and repository verification pass. A signed simulator app opens the empty local library with App Group access. The unsigned test build cannot access the App Group directly; store tests use temporary directories.
- PR #2 Brooks review: 100/100 with no actionable findings. The diff adds over 500 lines across 12 related files (app, tests, and docs), so review size remains a change-propagation signal to watch; generated Xcode project wiring was excluded from the assessment.
- PR #2 Codex review of `c9f7c03`: found a duplicate-row case when two saved URL forms resolve to the same video. CI Verify and Repository Verify passed on that head. The store fix and a fresh review are pending.
- PR #2 feedback fix: resolved workouts with the same video ID now coalesce into the earliest saved record, retaining available metadata. Two new regression cases pass; the local simulator suite is 18/18.
- PR #2 Codex re-review of `86c9f07`: found that an in-flight metadata result for a removed duplicate ID can be dropped. Both required CI checks passed on this head. The late-result fix and fresh review are pending. An earlier re-review attempt failed with a Git ref lookup error and was retried; the successful review found this issue.
- PR #2 second feedback fix: late enrichment now locates the surviving workout by resolved video ID when its original record ID was coalesced away. The regression test passes; local simulator tests are 19/19.
- PR #2 final `a95980e` review found no further issues; both checks passed, GitHub reported a clean merge, and the PR was squash-merged at `ea01404`.
- Milestone 3 integrated validation: 27 simulator tests pass, including player messages and notes migration. A signed simulator app loaded an invented workout from its App Group container. Live player controls could not be exercised through the locked Mac; they remain on the signed-iPhone checklist.
- PR #3 Brooks review: 100/100 with no actionable findings. The review sampled the player bridge, notes persistence, and tests across 12 related changed files; generated Xcode project wiring was excluded. The official player protocol is isolated in one bridge, and new storage behavior has regression tests. Live TikTok playback still requires signed-iPhone validation.
- PR #3 Codex review of `5078a6b` flagged player time events. TikTok's published table documents duration in `onCurrentTime`, but the review reports a ready-event duration shape. The bridge now accepts both and accepts current-time events without duration; focused and full simulator tests pass (28/28), as does repository verification. A fresh review is needed after the fix is pushed.
- PR #3 final `7057586` Codex review completed with no new findings. CI Verify and Repository Verify passed, GitHub reported a clean merge, and the PR was squash-merged at `b47b658`.
- Milestone 4 local validation: 38 simulator tests pass, including 10 backup cases for invalid archives, atomic restore, duplicate imports, and note preservation. A signed simulator app and share extension build pass. The signed empty library shows the Restore a backup action and Backup menu; actual Files export/import and live TikTok behavior still require a signed-iPhone check.
- PR #4 Brooks review: 100/100 with no actionable findings. The sampled review covered backup validation, the atomic additive merge, Files UI, and behavior tests across 11 related changed files; generated Xcode project wiring was excluded. The PR exceeds 500 changed lines, increasing review cost, but the change remains scoped to one backup milestone.
- PR #4 Codex review of `cb2a8c0` found six actionable cases: resolved-link duplicates, UUID collision order, quadratic matching, missing immediate enrichment, blank optional fields, and a file-size check that ran after loading. The prior head's checks passed; each finding was addressed in the commits below and acknowledged on the PR.
- PR #4 feedback fixes: restore now indexes matches, rejects conflicting UUIDs before post matches, and coalesces resolved duplicates while keeping current identity and notes. Backup import normalizes blank optional text and bounds file reading after a size precheck; the UI calls the regular refresh path after restore. The integrated simulator suite passes 44/44 tests, and the signed simulator app build passes. Fixes were pushed in `9d0141d` and `2b925e8`; see PR #4 for the final review and CI status.
- PR #4 Codex re-review of `743810e` found four further cases: source aliases lost across coalescence, untrusted thumbnail URLs, too many concurrent enrichment requests after a large restore, and unverified short-link resolutions joining unrelated current posts. The follow-up preserves locally confirmed aliases, discards unsafe imported URL claims, and queues enrichment with a three-request cap. Focused backup tests pass 18/18; the integrated iPhone simulator suite passes 50/50, the signed simulator build passes, and repository verification passes. A fresh PR review and CI run are required on the fix head.
- PR #4 Codex re-review of `cfa2a8e` found three more cases: growing alias sets rebuilt for every same-video entry, a local library over 10 MB could not be exported, and thumbnail refresh could replace saved titles or creators. The follow-up indexes one-record merges in place, allows complete local export with a warning when the 10 MB import limit is exceeded, and fills only missing metadata fields. Focused backup tests pass 20/20; the integrated simulator suite passes 53/53, the signed simulator build passes, and repository verification passes. A fresh PR review and CI run are required on the fix head.
- PR #5 Brooks review: 100/100 with no actionable findings. The App Intent capture path, local storage change, setup guidance, tests, project generator, and repository checks were sampled; generated Xcode project wiring was excluded. Live Share Sheet input still needs the physical iPhone check.
- PR #5 Codex review of `4a3cb60`: flagged existing App Group data becoming inaccessible after the entitlement is removed. The upgrade runbook now requires exporting a backup with the earlier signed app before installing the free-team build, then restoring and checking workouts and notes. README and sharing guidance link to the runbook. The current build cannot recover an old App Group without its entitlement; backups above the existing 10 MB import limit block upgrading until an import path is available.
- PR #5 direct-share revision: the Personal Team signed both app and Share extension with the same Keychain access group and no App Group. Focused signed-device Keychain and dual-inbox tests pass 4/4; the full revised suite passes 56 tests on the simulator (2 expected skips) and 57 on the physical iPhone (1 opt-in benchmark skip). The user confirmed TikTok Share → Save to Yarms added a workout, and its video played inline. The first player felt too small on iPhone 18 Pro Max, so a full-content-width player and larger playback buttons were installed for recheck.
- PR #5 Codex review of `b5c71f6`: found an orphaned PBXBuildFile in the app's Sources phase after removing an old Swift reference. Repository verification now rejects any build entry without a file or product; the generator removes stale build-phase entries before file references and cleans existing orphans. Verification failed before the fix and passed after regeneration; generation was idempotent, and the signed iPhone app build passed.
- PR #5 Brooks re-review: 100/100 with no actionable findings across the direct-share queue, local import, extension boundary, optional Shortcut compatibility, player sizing, tests, and generator. The generated Xcode project was excluded from Brooks scoring; Codex's concrete project-file finding was fixed and covered by repository verification. The physical playback layout and Files flow still need user-assisted confirmation.
- Compact-controls follow-up: a clean iPhone 18 Pro Max simulator passed 58 unit tests and 2 XCUITest flows (2 expected opt-in skips). UI automation covers paste into an empty or populated library, search, player/control dimensions, the always-visible TikTok fallback width, notes after scrolling and navigation, and backup menu access. The app now uses the system PasteButton to avoid a separate paste permission alert. The Xcode project generator is idempotent, repository verification passes, and a Personal Team signed generic iPhone build passes. Live TikTok playback and the Files picker are not covered by these simulator flows; the physical iPhone was unavailable for installation during this run.
- PR #6 Brooks review: 100/100 with no actionable findings. The review covered the player and library UI, new UI tests, CI configuration, generator logic, and docs; generated Xcode project wiring was excluded from Brooks scoring and validated by build and repository checks. The 12-file diff is scoped to one follow-up, with the project file accounting for most generated lines.
- PR #6 Codex review of `9fdacc1`: found that Paste was unavailable when a nonempty library search returned no workouts. The library now uses one system PasteButton callback in the empty, filtered, and populated states; a new XCUITest checks the filtered state. The focused regression test and full iPhone 18 Pro Max simulator suite pass (58 unit tests, 3 UI tests, 2 expected opt-in skips), as do repository verification and a signed generic iPhone build. A fresh PR review and CI run are required on the fix head.
- PR #6 CI diagnosis: the superseded macOS 15 run reached a failure in `testPasteSearchPlayerLayoutAndNotesPersistence` on an iOS 18.5 simulator, but its quiet log omitted the assertion and the run was canceled by the next push. CI Verify now preserves an Xcode result bundle and prints its test summary on failure so the next run can identify the specific problem. No CI failure is treated as resolved until a green run completes.
- PR #6 Codex review of `8213d5f`: found that UI tests used the normal simulator library and could leave fabricated workouts and notes behind. Each UI test now launches with a unique temporary library and inbox, skips the production Keychain queue, and requires a valid test-store UUID before using that path. A new unit test confirms path selection and invalid-argument fail-closed behavior. The full local iPhone 18 Pro Max simulator suite passes (59 unit tests, 3 UI tests, 2 expected opt-in skips), as do repository verification and the signed generic iPhone build. Fresh Codex review and CI Verify are pending.
- PR #6 CI Verify on `c1dfb15` failed only in `testPasteSearchPlayerLayoutAndNotesPersistence` on iOS 18.5: 59 tests passed, 2 skipped, and one assertion failed. The Xcode summary reported `XCTAssertTrue failed` without a source line. The workflow now prints detailed results for each failing test; the exact assertion and corrective change are pending the next run.
- PR #6 CI Verify on `c16f14c` reported the same assertion failure; `xcresulttool test-details` still omitted a source line. The main UI flow now labels each assertion, and CI will print the failed test's recent XCTest activities. The narrower iPhone 17e simulator run on iOS 26.5 passes, so screen width alone does not reproduce this runner difference.
- PR #6 CI Verify on `f425bed` identified the failure as the transient “Saved on this iPhone” UI label assertion on iOS 18.5. The test already checks the durable result by leaving and reopening the workout and comparing note text, so the transient-label assertion is removed. This does not change note storage or user-visible app behavior; the next full CI run must prove persistence on iOS 18.5.
